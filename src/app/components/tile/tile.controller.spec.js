'use strict';

describe('controllers', function(){
  var scope;

  beforeEach(module('oneforty'));

  beforeEach(inject(function($rootScope) {
    scope = $rootScope.$new();
  }));

  it('should blablabla', inject(function($controller) {

  }));
});
